[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "kpl"
version = "0.1.0"
description = "Kikuyu Monkey interpreter"
readme = "README.md"
requires-python = ">=3.12"
